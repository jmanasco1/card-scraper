__pycache__/
*.pyc

# eBay price cache (local, regenerated per run)
results/.price_cache.json

# local virtualenv created by run.sh
.venv/

# persistent browser profile (holds your eBay session — never commit)
.browser-profile/
