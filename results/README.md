Scan output lands here (`summary_<sport>.md`, `latest_<sport>.csv/.json`).

The previous results committed here were removed: they came from the old
population + gem-rate ranking, which surfaced famous vintage cards rather than
mispriced ones, and every row in them had zero price comps. Keeping them would
just be a misleading example of the output format.

Run the scan locally to regenerate — see the README at the repo root.
